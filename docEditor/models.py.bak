from django.db import models

# Create your models here.
class TextEditor(models.Model):
	editor_text = models.CharField(max_length=10000)

class GDocEditor(models.Model):
	text = models.CharField(max_length=10000)